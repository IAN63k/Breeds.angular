import { Routes } from "@angular/router";
import { BreedListComponent } from "./breed-list/breed-list.component";

const routes: Routes = [
    { path: '', component: BreedListComponent },      // /breeds
    // { path: 'favorites', component: FavoritesComponent }
];